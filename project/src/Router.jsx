import { BrowserRouter, Route, Routes } from "react-router-dom";

export const Router = () => {
  return (
    <BrowserRouter>
      <Routes>{/*Route 추가해서 사용해주세요*/}</Routes>
    </BrowserRouter>
  );
};
